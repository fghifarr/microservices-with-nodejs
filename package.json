{
  "name": "microservices-with-nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd api* && npm start\" \"cd user* && npm start\"",
    "dev": "concurrently \"cd api* && npm run dev\" \"cd user* && npm run dev\"",
    "install": "concurrently \"cd api* && npm i\" \"cd user* && npm i\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fathullah Ghifar",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1"
  }
}
